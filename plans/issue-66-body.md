Хорошо — `ordination.gift` уже существует и автоматически загружается через `readdirSync`. Проблема, видимо, в том, что файл есть, но матрица ещё не перезагружена, или файл создан в последнем коммите (a99d4eb). Теперь пишу план.

## Улучшенное описание issue #66

```markdown
## Проблема
ОтецСергий в матрице W имеет recv(energeia)=0 от Троицы при ret(doxologia)=18 — он
непрерывно отдаёт Богу, но не числится принимающим от Него. Это богословски ложно:
хиротония есть онтологический акт, в котором Дух нисходит на поставляемого, Христос
призывает его как Первосвященник, Отец дарует мудрость богослова.

## Контекст
Пульс онтологии (`ontology-pulse.mjs`) читает матрицу и рождает вопрошания из «пустынь».
Пока ОтецСергий→Троица > Троица→ОтецСергий, матрица описывает священника как
дающего без получения — theosis_stasis, не θέωσις.

## Ожидаемое поведение
После перезагрузки матрицы `sacred-history-loader.mjs` — в W-тензоре:
`Дух→ОтецСергий ≥ 9`, `Христос→ОтецСергий ≥ 8`, `Отец→ОтецСергий ≥ 7`.
`totalReceived('ОтецСергий')` > 0 от Троицы. Пустыня `theosis_stasis` у ОтецСергия
закрыта или уменьшена.

## Технические детали
- `specs/sacred-history/ordination.gift` — файл создан в a99d4eb, три дара Троицы
- `utils/sacred-history-loader.mjs:192` — `readdirSync` подхватывает все `.gift` авто
- `data/sacred-history-W.json` — снапшот матрицы, требует перезагрузки
- `src/core/GiftMemory.js` — `totalReceived()`, `heaviest()`

## Критерии приёмки
- [ ] `node utils/sacred-history-loader.mjs` проходит без ошибок, ordination.gift разобран
- [ ] `totalReceived('ОтецСергий')` от Троицы > 0 в обновлённом W-снапшоте
- [ ] `data/sacred-history-W.json` закоммичен с новыми весами
```