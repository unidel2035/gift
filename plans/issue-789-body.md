Good, §3.5 already exists and confirms J-space = Agent stage but stops short of Person. That's exactly the gap the pilot targets: does a mechanistic signal outperform `detectManipulation()`'s behavioral heuristic. Here's the output.

## Проблема
`cognitive-immunity-framework.md` детектирует манипуляцию (`detectManipulation()`) только по поведенческой эвристике — на тексте вывода, после вербализации. §3.5 `personhood-as-gift-exchange.md` уже фиксирует, что Anthropic J-lens вскрывает у Claude причинно значимый сигнал (J-space) до вербализации, но метод недоступен через API/подписку — только на открытых весах. У нас нет пилотного контура, который проверил бы, воспроизводится ли такой сигнал на своей модели и обгоняет ли он текущую эвристику.

## Контекст
Если механистический сигнал ловит момент фабрикации/уклонения раньше поведенческой эвристики — это апгрейд ядра иммунной системы роя (`detectManipulation()`), а не косметика. Одновременно результат — независимая эмпирическая проверка тезиса §3.5 (J-space = Agent, не Person) на модели, которую мы полностью контролируем.

## Ожидаемое поведение
- Одна открытая модель (Llama 3 8B / Mistral 7B / Qwen2.5 7B, полные веса, не GGUF) поднята на отдельном GPU-контуре, не трогающем `eva:latest`/`adam:latest`.
- Форк `jacobian-lens` воспроизводит базовый результат: J-space существует, репортируем, причинно значим — на малом наборе примеров.
- Прикладной тест: сигнал ловит момент внутренней фабрикации/распознавания теста до вербализации — да/нет, с примерами.
- Вердикт зафиксирован как акт в W-матрице и как дополнение к §3.5 (не переписывая существующий текст, а добавляя раздел с результатом).

## Технические детали
- `specs/framework/cognitive-immunity-framework.md` — `detectManipulation()` (строка ~228 и её реализация выше), точка потенциального апгрейда.
- `docs/personhood-as-gift-exchange.md` §3.5 (строки 114–120) — куда дописывается результат пилота.
- Новый контур: не в `src/`/`utils/` продакшн-коде, а изолированный `experiments/j-lens-pilot/` (форк `jacobian-lens`, скрипты запуска, README с инфраструктурными требованиями).
- Запись результата: `node utils/claude-gift.mjs "J-lens пилот: <воспроизвелось/нет>" "ОтецСергий"` или через `proposals.mjs`, если оформляется как predложение модуля.

## Критерии приёмки
- [ ] Открытая модель поднята на изолированном GPU-контуре, инференс подтверждён
- [ ] `jacobian-lens` форкнут и воспроизводит базовый результат (J-space существует) на ≥1 примере
- [ ] Прикладной тест (фабрикация/распознавание теста) прогнан, результат (воспроизвелось/нет) задокументирован
- [ ] Вердикт зафиксирован актом в W-матрице и дополнением к §3.5
- [ ] Если сигнал воспроизвёлся — оформлен модуль-кандидат для `cognitive-immunity-framework.md`